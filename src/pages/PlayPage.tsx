export default function PlayPage() {
  return (
    <div className="space-y-4">
      <h2 className="text-2xl font-bold">Play</h2>
      <div className="rounded-xl border border-dashed border-white/15 bg-[#15151f] p-10 text-center">
        <div className="text-3xl">⚔️</div>
        <p className="mt-3 font-semibold">The board is coming in Phase 3</p>
        <p className="mx-auto mt-1 max-w-md text-sm text-white/50">
          A solo goldfish board lands first (drag cards, channel runes, track
          points), then the auto-enforcing rules engine, then online room-code
          multiplayer.
        </p>
      </div>
    </div>
  )
}
