# Handler-coverage analysis — where to apply our learnings

> **What this is.** An objective, pool-wide map of which cards the engine already
> resolves, which it punts to `manual`, and — for the manual ones — which mechanic
> family they belong to, so we know exactly which cards a given handler would unlock.
> Generated by running the real engine parsers (`cardIntent.prefillSpecFromCard` →
> `effects.ts`/`keywords.ts`/`triggers.ts`) over all **984** cards. Raw per-card data:
> [`handler-coverage.data.json`](handler-coverage.data.json). Pairs with the meta-ranked
> [`gap-matrix.md`](gap-matrix.md) (impact = how many of the 10 meta decks need a mechanic)
> and the slot grammar in [`card-grammar.md`](card-grammar.md).

## Method & caveats
- For each card we record: the `ParsedEffect` mechanic keys the parser detects, the
  keywords, the triggers, a `manual` flag (the parser couldn't resolve some text), and
  whether the card is **hand-coded** (a bespoke per-card branch).
- `handCoded` is detected by the card's bare name appearing as a quoted literal in
  `engine.ts`/`battlefieldScripts.ts`. This catches name-keyed handlers (≈99 cards) but
  **misses the few regex-keyed ones** (Lee Sin - Centered, Garen - Commander) — treat the
  hand-coded count as a floor.
- A card is effectively **covered** if it's `fully-parsed`, `keyword-only`, `vanilla`, or
  `handCoded`. The genuine work surface is **`manual`/`partial` AND not hand-coded ≈ 275 cards.**

## Coverage summary (984 cards)
| Bucket | Count | % | Meaning |
|---|--:|--:|---|
| `fully-parsed` | 490 | 50% | every text clause maps to a `ParsedEffect` key |
| `keyword-only` | 109 | 11% | only `[Keyword]`s (Tank/Assault/…), already handled |
| `manual-unhandled` | 332 | 34% | text present, parser extracted nothing usable |
| `partial` | 29 | 3% | some clauses parsed, some flagged `manual` |
| `vanilla` | 24 | 2% | no rules text (basic runes etc.) |
| hand-coded | 99 | — | bespoke per-card/ -battlefield handlers (overlaps the above) |

So **~63% of the pool resolves on its own** (parsed + keyword + vanilla), bespoke code covers
more of the rest, leaving **~290 cards** (manual/partial and not hand-coded) that need a
handler — clustered into the families below.

> **Important nuance (discovered this pass): the scan measures PARSE coverage, not RESOLUTION.**
> A card can be `fully-parsed` (the parser understood the text) yet not be *resolved* by the
> reducer. This matters most for **activated abilities** — see below.

## Activated abilities — the real story (updated)
Activated abilities (`<cost>: <effect>`) are **already detected and offered generically**
(`unitActivatedAbility` + `ACTIVATE_UNIT`); **167 cards** have one. `ACTIVATE_UNIT` resolves a
growing inline set of effects: damage, tempMight, buff, move-to-base, return-to-hand, draw,
channel, recruits, named/gold tokens, predict, gainXP, **`[Add]` resources** (rune-ramp: the
Seals, Energy Conduit, Malzahar - Fanatic — add Power/Energy to the pool), and — added in the
#1-continuation pass — **stun, kill, grant-Assault/Ganking, ready (a unit / your units / self /
runes / others-here), and channel-exhausted**. So the bulk of the formerly-stuck activated
cards (the ready/stun/kill/grant/channel-exhausted family) now actually resolve; targeting for
the new targeted effects (stun/kill/grant/ready-unit) is wired in both MatchPage and OnlinePage.
**Still unresolved** (parse but don't yet resolve via activation): **score, move-to-battlefield
(move an enemy/unit to a battlefield), dealMight, copy.** These are the remaining activated tail
and overlap with the gap-matrix mechanics ranked below. Known limitation: `[Add]`/Seal abilities
are printed at **[Reaction] speed** but `ACTIVATE_UNIT` still gates on `requireActiveAction`
(your turn only), so a Seal can't be tapped mid-chain / on the opponent's turn yet.

## By mechanic family — "apply this learning to these cards"
Counts are over the 341 manual/partial cards (full lists in the dataset). "Effect status" =
does the EFFECT verb already parse? (so the gap is usually the trigger/activated wrapper or a
condition, not the verb).

| Family | # | Effect status | Representative manual cards | Note |
|---|--:|---|---|---|
| **Ready / exhaust units** | 70 | ✅ `readyUnits`/`readyAll` | Magma Wurm, Darius - Trifarian, Mageseeker Warden, Solari Shrine | mostly **activated** (see lever) |
| **Buff / +Might (permanent)** | 68 | ✅ `buff` | Captain Farron, Wielder of Water, Block | activated/triggered + conditions |
| **This-turn Might** | 53 | ✅ `tempMight*` | Brynhir Thundersong, Raging Firebrand, Ravenborn Tome | activated/conditional wrapper |
| **Tokens (recruit/gold/named)** | 35 | ✅ `recruits`/`namedToken` | Viktor - Herald, Assembly Rig | named-token variants + activated |
| **Kill / destroy** | 33 | ✅ `kill` | Thermo Beam, Last Stand, Fading Memories | activated/conditional |
| **Recall (to base)** | 27 | ✅ `moveToBase` | Portal Rescue, The Syren, Possession | activated wrapper |
| **Bounce (return to hand)** | 23 | ✅ `bounce` | Gust, Rebuke, Zaunite Bouncer | activated/triggered wrapper |
| **Deck-dig (predict/vision/look)** | 19 | ⚠️ `peek*` partial | Gemcraft Seer, Mystic Poro, Karma - Channeler | Predict simplified; conditional reveal ❌ |
| **Draw** | 18 | ✅ `draw` | Clockwork Keeper, Treasure Trove | activated/conditional |
| **Move (Charm-style / move-with)** | 17 | ⚠️ `moveUnit` ✅ but enemy→bf gaps | The Syren, Yasuo - Unforgiven, Stealthy Pursuer | **move enemy to a chosen bf** ❌ (gap-matrix #7) |
| **Replacement (instead / "would")** | 16 | ❌ no `replacement` KIND | Noxian Guillotine, Sett - The Boss, Symbol of the Solari | fully-missing kind (gap-matrix #6) |
| **Cost-shaping auras** | 19 | ⚠️ static only | Eager Apprentice, Herald of Scales, Void Drone | conditional/aura `costMod` ❌ |
| **Channel / runes** | 12 | ✅ `channel*` | Treasure Trove, Albus Ferros | activated wrapper |
| **Discard** | 11 | ❌ `discard` effect | Mindsplitter, Super Mega Death Rocket! | + reveal-hand/force-discard ❌ |
| **Heal / damage-prevent** | 9 | ❌ | Unyielding Spirit, Soraka - Wanderer | part of the replacement layer |
| **"Deal damage = Might"** | 15 | ⚠️ `strikeDown` bespoke | Caitlyn, Carnivorous Snapvine, Stormbringer | generalize the Might-as-damage primitive |
| **Trash recursion (play-from-trash)** | 8 | ❌ `playFrom` | Rhasa the Sunderer, Hallowed Tomb | gap-matrix **#1** (5 meta decks) |
| **Counter a spell** | 8 | ⚠️ chain counter ✅ | Defy, Wind Wall, Riposte, Not So Fast | "counter **unless** pay N" ❌ |
| **Stat-scaling (dynamic Might)** | 8 | ❌ long tail | Draven - Showboat, Stormbringer | keep bespoke |
| **Copy / transform** | 5 | ⚠️ bespoke | Mirror Image, LeBlanc variants | keep bespoke |
| **Score points** | 2 | ❌ `score` | Leona - Zealot, Renata - Mastermind | gap-matrix #8 |
| **On-discard / on-spend trigger** | 2 | ❌ event | Flame Chompers, Darius - Trifarian | add trigger events |

**Notably unclustered (their own small families):** copy-all-abilities (Heimerdinger),
counter/Reaction spells (Defy, Wind Wall, Mystic Reversal), cost-reduction auras (Eager
Apprentice, Herald of Scales), "play me to an open battlefield" (Sneaky Deckhand), conditional
move-with (Stealthy Pursuer), extra-turn (Time Warp), reveal-hand (Sabotage, Mindsplitter).

## Hand-coded → generalizable
66 cards are hand-coded yet still parser-`manual` (correctly — they need bespoke logic). A few
are worth **generalizing into a reusable handler** because the pool has many siblings:
- **"deal damage equal to its/my Might"** — Yasuo - Remorseful, Yone - Blademaster, Warwick are
  bespoke; ≥15 cards share the phrasing → a `dealMight` effect primitive.
- **Reflection / copy** — Mirror Image, LeBlanc, Keeper of Masks each bespoke → one `copy` effect.
- **stat-scaling Might** (Draven points / Mundo trash) → a small `statScale(source)` primitive.
- Most other hand-coded cards are genuine one-offs; leave them.

## Prioritized fix queue (pool reach × meta impact)
1. ~~**Resolve the rest of the activated-ability effects**~~ — **largely DONE.** `[Add]`
   resources (~41 cards) plus **stun / kill / grant-Assault·Ganking / ready (unit·your-units·
   self·runes·here) / channel-exhausted** now resolve via `ACTIVATE_UNIT`, with targeting wired
   in both pages. **Remaining activated tail:** `score`, `move-to-battlefield`, `dealMight`,
   `copy` — folded into items 5/6/8 below. (Also deferred: `[Reaction]`-speed for `[Add]`/Seals.)
2. **Play-from-deck/trash for free** — gap-matrix **#1** (5 meta decks) + the trash-recursion
   family; a fully-missing `playFrom` effect.
3. **Replacement / "would die" layer** (heal / recall / prevent) — gap-matrix #6; the only
   fully-missing KIND; unlocks Sett, Zhonya's, Guardian Angel + ~16 cards.
4. **Stun triggers + "if target stunned"** — gap-matrix #3 (4 decks).
5. **Move an enemy unit to a chosen battlefield** — gap-matrix #7; extend `moveUnit` past the
   Charm case to the move family's enemy/with variants.
6. **Direct `score(N)` + `on.winCombat`** — gap-matrix #8.
7. **Enemy-death triggers** (`on.death.enemy`) — gap-matrix #5.
8. **`dealMight` + `copy` primitives** — generalize the bespoke handlers above (~20 cards).
9. **Counter "unless pay N"** + conditional/aura cost-shaping — Hard Bargain, Vex, ~19 aura cards.
10. **`discard` effect + reveal-hand** — Mindsplitter family + on-discard trigger event.

## Using / regenerating
The dataset was produced by a throwaway env-gated generator (`GEN_COVERAGE=1 npx vitest run`
over a temp file importing `cardIntent` + `CARDS`); it's not part of the standing suite. Filter
[`handler-coverage.data.json`](handler-coverage.data.json) by `bucket` / `mechanics` /
`handCoded` to get the exact card list for any fix. Implementing these (the **fix campaign**) is
the next, separate effort — start at #1 (activated abilities) for the most cards-per-hour.
